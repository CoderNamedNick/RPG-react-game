const CharacterData = ({ playerName }) => {
  const data = {
    PlayerName: playerName
  };

  // You might use `data` somewhere in your component logic or return it

  // For example, if you want to render it:
  return null;
};

export default CharacterData;